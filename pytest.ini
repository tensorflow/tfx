[pytest]
addopts = --verbose -m "not end_to_end" --import-mode=importlib
testpaths = tfx
python_files = *_test.py
norecursedirs = custom_components .* *.egg
markers =
    e2e: end to end tests which are slow and requires more dependency (deselect with '-m "not e2e"')
    serial
