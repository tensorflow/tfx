version: "3"

services:
  
  tfx:
    build:
      context: setup/
      dockerfile: Dockerfile
    ports:
    - "8080:8080" # airflow
    - "8000:8000" # jupyter notebook
    volumes:
    - "./data:/home/data"
    - "./airflow_data:/home/airflow"
